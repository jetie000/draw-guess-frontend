export enum UserRoles {
  User = 0,
  Admin = 1,
  Moderator = 2
}

export enum AccountTypes {
  Email = 0,
  Google = 1
}

export enum LeaderboardTypes {
  Points = 'points',
  Wins = 'wins',
  WordsGuessed = 'words-guessed'
}
